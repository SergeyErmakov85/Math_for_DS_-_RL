# CSS

Файлы этого раздела физически лежат внутри хранилища — в `Vault/.obsidian/snippets/`.

Причина в устройстве Obsidian: он видит только содержимое своей папки. Хранить рабочую копию здесь и вторую внутри хранилища означало бы два источника истины и неизбежное расхождение между ними.

Что там находится: сниппеты `enf-tokens.css`, `enf-math.css`, `enf-callouts.css`. Все три порождаются командой `node Scripts/gen-obsidian-css.mjs` из источников в [`../../css/`](../../css/) и [`../../snippets/`](../../snippets/); правка копии внутри хранилища теряется при следующей генерации.

Открыть: [`../Vault/`](../Vault/) как хранилище Obsidian. Инструкция — [`../Vault/README.md`](../Vault/README.md).
